#pragma once
#include <string>

struct StrStr
{
    std::string first;
    std::string second;
};
struct IntStr
{
    int first;
    std::string second;
};
struct StrInt
{
    std::string first;
    int second;
};
struct IntInt
{
    int first;
    int second;
};
